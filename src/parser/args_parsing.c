/*
** EPITECH PROJECT, 2024
** Corewar
** File description:
** parsing.c
*/

#include "struct.h"

parsed_t *parse_args(int ac, char **av)
{
    parsed_t *parsed = malloc(sizeof(parsed_t));

    if (parsed == NULL)
        return NULL;
}
